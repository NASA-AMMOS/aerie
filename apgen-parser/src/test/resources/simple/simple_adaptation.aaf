apgen version 1.0

activity type Banana
    begin
    attributes
        "Color" = "Yellow";
        "Duration" = duration;
     parameters
         duration: Duration default to 00:30:00;
     end activity type Banana

activity type Apple
    begin
    attributes
        "Duration" = 01:00:00;
    parameters
        parameters: array default to ["red", 3.0, 4];
    end activity type Apple

activity type Avocado
    begin
    parameters
        softness: float default to 1.0;
        size: string default to "medium" range [ "small", "medium", "large" ];
        age: integer default to 7;
        expiration: Time default to 2019-365T23:59:59;
    end activity type Avocado