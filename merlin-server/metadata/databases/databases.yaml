- name: Merlin
  kind: postgres
  configuration:
    connection_info:
      database_url:
        from_env: MERLIN_DATABASE_URL
      isolation_level: read-committed
      use_prepared_statements: false
  tables: "!include Merlin/tables/tables.yaml"
