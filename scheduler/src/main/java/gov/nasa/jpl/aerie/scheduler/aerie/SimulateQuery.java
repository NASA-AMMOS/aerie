// AUTO-GENERATED FILE. DO NOT MODIFY.
//
// This class was automatically generated by Apollo GraphQL plugin from the GraphQL queries it found.
// It should not be modified by hand.
//
package gov.nasa.jpl.aerie.scheduler.aerie;

import com.apollographql.apollo.api.Operation;
import com.apollographql.apollo.api.OperationName;
import com.apollographql.apollo.api.Query;
import com.apollographql.apollo.api.Response;
import com.apollographql.apollo.api.ResponseField;
import com.apollographql.apollo.api.ScalarTypeAdapters;
import com.apollographql.apollo.api.internal.InputFieldMarshaller;
import com.apollographql.apollo.api.internal.InputFieldWriter;
import com.apollographql.apollo.api.internal.OperationRequestBodyComposer;
import com.apollographql.apollo.api.internal.QueryDocumentMinifier;
import com.apollographql.apollo.api.internal.ResponseFieldMapper;
import com.apollographql.apollo.api.internal.ResponseFieldMarshaller;
import com.apollographql.apollo.api.internal.ResponseReader;
import com.apollographql.apollo.api.internal.ResponseWriter;
import com.apollographql.apollo.api.internal.SimpleOperationResponseParser;
import com.apollographql.apollo.api.internal.UnmodifiableMapBuilder;
import com.apollographql.apollo.api.internal.Utils;
import gov.nasa.jpl.aerie.scheduler.aerie.type.CustomType;
import okio.Buffer;
import okio.BufferedSource;
import okio.ByteString;
import org.jetbrains.annotations.NotNull;
import org.jetbrains.annotations.Nullable;

import java.io.IOException;
import java.util.Collections;
import java.util.LinkedHashMap;
import java.util.List;
import java.util.Map;

public final class SimulateQuery implements Query<SimulateQuery.Data, SimulateQuery.Data, SimulateQuery.Variables> {
  public static final String OPERATION_ID = "124e6e48702a5d8bd5894447bec99f9545d5e53e49c975ae50533810d7cac613";

  public static final String QUERY_DOCUMENT = QueryDocumentMinifier.minify(
    "query Simulate($adaptationId: String!, $planId: String!) {\n"
        + "  simulate(adaptationId: $adaptationId, planId: $planId) {\n"
        + "    __typename\n"
        + "    success\n"
        + "    results {\n"
        + "      __typename\n"
        + "      name\n"
        + "      start\n"
        + "      values {\n"
        + "        __typename\n"
        + "        x\n"
        + "        y\n"
        + "      }\n"
        + "    }\n"
        + "    activities {\n"
        + "      __typename\n"
        + "      duration\n"
        + "      startTimestamp\n"
        + "      type\n"
        + "      parameters {\n"
        + "        __typename\n"
        + "        name\n"
        + "        value\n"
        + "      }\n"
        + "    }\n"
        + "  }\n"
        + "}"
  );

  public static final OperationName OPERATION_NAME = new OperationName() {
    @Override
    public String name() {
      return "Simulate";
    }
  };

  private final Variables variables;

  public SimulateQuery(@NotNull String adaptationId, @NotNull String planId) {
    Utils.checkNotNull(adaptationId, "adaptationId == null");
    Utils.checkNotNull(planId, "planId == null");
    variables = new Variables(adaptationId, planId);
  }

  @Override
  public String operationId() {
    return OPERATION_ID;
  }

  @Override
  public String queryDocument() {
    return QUERY_DOCUMENT;
  }

  @Override
  public Data wrapData(Data data) {
    return data;
  }

  @Override
  public Variables variables() {
    return variables;
  }

  @Override
  public ResponseFieldMapper<Data> responseFieldMapper() {
    return new Data.Mapper();
  }

  public static Builder builder() {
    return new Builder();
  }

  @Override
  public OperationName name() {
    return OPERATION_NAME;
  }

  @Override
  @NotNull
  public Response<Data> parse(@NotNull final BufferedSource source,
                              @NotNull final ScalarTypeAdapters scalarTypeAdapters) throws IOException {
    return SimpleOperationResponseParser.parse(source, this, scalarTypeAdapters);
  }

  @Override
  @NotNull
  public Response<Data> parse(@NotNull final ByteString byteString,
                              @NotNull final ScalarTypeAdapters scalarTypeAdapters) throws IOException {
    return parse(new Buffer().write(byteString), scalarTypeAdapters);
  }

  @Override
  @NotNull
  public Response<Data> parse(@NotNull final BufferedSource source) throws
      IOException {
    return parse(source, ScalarTypeAdapters.DEFAULT);
  }

  @Override
  @NotNull
  public Response<Data> parse(@NotNull final ByteString byteString) throws
      IOException {
    return parse(byteString, ScalarTypeAdapters.DEFAULT);
  }

  @Override
  @NotNull
  public ByteString composeRequestBody(@NotNull final ScalarTypeAdapters scalarTypeAdapters) {
    return OperationRequestBodyComposer.compose(this, false, true, scalarTypeAdapters);
  }

  @NotNull
  @Override
  public ByteString composeRequestBody() {
    return OperationRequestBodyComposer.compose(this, false, true, ScalarTypeAdapters.DEFAULT);
  }

  @Override
  @NotNull
  public ByteString composeRequestBody(final boolean autoPersistQueries,
      final boolean withQueryDocument, @NotNull final ScalarTypeAdapters scalarTypeAdapters) {
    return OperationRequestBodyComposer.compose(this, autoPersistQueries, withQueryDocument, scalarTypeAdapters);
  }

  public static final class Builder {
    private @NotNull String adaptationId;

    private @NotNull String planId;

    Builder() {
    }

    public Builder adaptationId(@NotNull String adaptationId) {
      this.adaptationId = adaptationId;
      return this;
    }

    public Builder planId(@NotNull String planId) {
      this.planId = planId;
      return this;
    }

    public SimulateQuery build() {
      Utils.checkNotNull(adaptationId, "adaptationId == null");
      Utils.checkNotNull(planId, "planId == null");
      return new SimulateQuery(adaptationId, planId);
    }
  }

  public static final class Variables extends Operation.Variables {
    private final @NotNull String adaptationId;

    private final @NotNull String planId;

    private final transient Map<String, Object> valueMap = new LinkedHashMap<>();

    Variables(@NotNull String adaptationId, @NotNull String planId) {
      this.adaptationId = adaptationId;
      this.planId = planId;
      this.valueMap.put("adaptationId", adaptationId);
      this.valueMap.put("planId", planId);
    }

    public @NotNull String adaptationId() {
      return adaptationId;
    }

    public @NotNull String planId() {
      return planId;
    }

    @Override
    public Map<String, Object> valueMap() {
      return Collections.unmodifiableMap(valueMap);
    }

    @Override
    public InputFieldMarshaller marshaller() {
      return new InputFieldMarshaller() {
        @Override
        public void marshal(InputFieldWriter writer) throws IOException {
          writer.writeString("adaptationId", adaptationId);
          writer.writeString("planId", planId);
        }
      };
    }
  }

  /**
   * Data from the response after executing this GraphQL operation
   */
  public static class Data implements Operation.Data {
    static final ResponseField[] $responseFields = {
      ResponseField.forObject("simulate", "simulate", new UnmodifiableMapBuilder<String, Object>(2)
      .put("adaptationId", new UnmodifiableMapBuilder<String, Object>(2)
        .put("kind", "Variable")
        .put("variableName", "adaptationId")
        .build())
      .put("planId", new UnmodifiableMapBuilder<String, Object>(2)
        .put("kind", "Variable")
        .put("variableName", "planId")
        .build())
      .build(), true, Collections.<ResponseField.Condition>emptyList())
    };

    final @Nullable Simulate simulate;

    private transient volatile String $toString;

    private transient volatile int $hashCode;

    private transient volatile boolean $hashCodeMemoized;

    public Data(@Nullable Simulate simulate) {
      this.simulate = simulate;
    }

    public @Nullable Simulate simulate() {
      return this.simulate;
    }

    @SuppressWarnings({"rawtypes", "unchecked"})
    public ResponseFieldMarshaller marshaller() {
      return new ResponseFieldMarshaller() {
        @Override
        public void marshal(ResponseWriter writer) {
          writer.writeObject($responseFields[0], simulate != null ? simulate.marshaller() : null);
        }
      };
    }

    @Override
    public String toString() {
      if ($toString == null) {
        $toString = "Data{"
          + "simulate=" + simulate
          + "}";
      }
      return $toString;
    }

    @Override
    public boolean equals(Object o) {
      if (o == this) {
        return true;
      }
      if (o instanceof Data) {
        Data that = (Data) o;
        return ((this.simulate == null) ? (that.simulate == null) : this.simulate.equals(that.simulate));
      }
      return false;
    }

    @Override
    public int hashCode() {
      if (!$hashCodeMemoized) {
        int h = 1;
        h *= 1000003;
        h ^= (simulate == null) ? 0 : simulate.hashCode();
        $hashCode = h;
        $hashCodeMemoized = true;
      }
      return $hashCode;
    }

    public static final class Mapper implements ResponseFieldMapper<Data> {
      final Simulate.Mapper simulateFieldMapper = new Simulate.Mapper();

      @Override
      public Data map(ResponseReader reader) {
        final Simulate simulate = reader.readObject($responseFields[0], new ResponseReader.ObjectReader<Simulate>() {
          @Override
          public Simulate read(ResponseReader reader) {
            return simulateFieldMapper.map(reader);
          }
        });
        return new Data(simulate);
      }
    }
  }

  public static class Simulate {
    static final ResponseField[] $responseFields = {
      ResponseField.forString("__typename", "__typename", null, false, Collections.<ResponseField.Condition>emptyList()),
      ResponseField.forBoolean("success", "success", null, false, Collections.<ResponseField.Condition>emptyList()),
      ResponseField.forList("results", "results", null, true, Collections.<ResponseField.Condition>emptyList()),
      ResponseField.forList("activities", "activities", null, true, Collections.<ResponseField.Condition>emptyList())
    };

    final @NotNull String __typename;

    final boolean success;

    final @Nullable List<Result> results;

    final @Nullable List<Activity> activities;

    private transient volatile String $toString;

    private transient volatile int $hashCode;

    private transient volatile boolean $hashCodeMemoized;

    public Simulate(@NotNull String __typename, boolean success, @Nullable List<Result> results,
        @Nullable List<Activity> activities) {
      this.__typename = Utils.checkNotNull(__typename, "__typename == null");
      this.success = success;
      this.results = results;
      this.activities = activities;
    }

    public @NotNull String __typename() {
      return this.__typename;
    }

    public boolean success() {
      return this.success;
    }

    public @Nullable List<Result> results() {
      return this.results;
    }

    public @Nullable List<Activity> activities() {
      return this.activities;
    }

    @SuppressWarnings({"rawtypes", "unchecked"})
    public ResponseFieldMarshaller marshaller() {
      return new ResponseFieldMarshaller() {
        @Override
        public void marshal(ResponseWriter writer) {
          writer.writeString($responseFields[0], __typename);
          writer.writeBoolean($responseFields[1], success);
          writer.writeList($responseFields[2], results, new ResponseWriter.ListWriter() {
            @Override
            public void write(List items, ResponseWriter.ListItemWriter listItemWriter) {
              for (Object item : items) {
                listItemWriter.writeObject(((Result) item).marshaller());
              }
            }
          });
          writer.writeList($responseFields[3], activities, new ResponseWriter.ListWriter() {
            @Override
            public void write(List items, ResponseWriter.ListItemWriter listItemWriter) {
              for (Object item : items) {
                listItemWriter.writeObject(((Activity) item).marshaller());
              }
            }
          });
        }
      };
    }

    @Override
    public String toString() {
      if ($toString == null) {
        $toString = "Simulate{"
          + "__typename=" + __typename + ", "
          + "success=" + success + ", "
          + "results=" + results + ", "
          + "activities=" + activities
          + "}";
      }
      return $toString;
    }

    @Override
    public boolean equals(Object o) {
      if (o == this) {
        return true;
      }
      if (o instanceof Simulate) {
        Simulate that = (Simulate) o;
        return this.__typename.equals(that.__typename)
         && this.success == that.success
         && ((this.results == null) ? (that.results == null) : this.results.equals(that.results))
         && ((this.activities == null) ? (that.activities == null) : this.activities.equals(that.activities));
      }
      return false;
    }

    @Override
    public int hashCode() {
      if (!$hashCodeMemoized) {
        int h = 1;
        h *= 1000003;
        h ^= __typename.hashCode();
        h *= 1000003;
        h ^= Boolean.valueOf(success).hashCode();
        h *= 1000003;
        h ^= (results == null) ? 0 : results.hashCode();
        h *= 1000003;
        h ^= (activities == null) ? 0 : activities.hashCode();
        $hashCode = h;
        $hashCodeMemoized = true;
      }
      return $hashCode;
    }

    public static final class Mapper implements ResponseFieldMapper<Simulate> {
      final Result.Mapper resultFieldMapper = new Result.Mapper();

      final Activity.Mapper activityFieldMapper = new Activity.Mapper();

      @Override
      public Simulate map(ResponseReader reader) {
        final String __typename = reader.readString($responseFields[0]);
        final boolean success = reader.readBoolean($responseFields[1]);
        final List<Result> results = reader.readList($responseFields[2], new ResponseReader.ListReader<Result>() {
          @Override
          public Result read(ResponseReader.ListItemReader listItemReader) {
            return listItemReader.readObject(new ResponseReader.ObjectReader<Result>() {
              @Override
              public Result read(ResponseReader reader) {
                return resultFieldMapper.map(reader);
              }
            });
          }
        });
        final List<Activity> activities = reader.readList($responseFields[3], new ResponseReader.ListReader<Activity>() {
          @Override
          public Activity read(ResponseReader.ListItemReader listItemReader) {
            return listItemReader.readObject(new ResponseReader.ObjectReader<Activity>() {
              @Override
              public Activity read(ResponseReader reader) {
                return activityFieldMapper.map(reader);
              }
            });
          }
        });
        return new Simulate(__typename, success, results, activities);
      }
    }
  }

  public static class Result {
    static final ResponseField[] $responseFields = {
      ResponseField.forString("__typename", "__typename", null, false, Collections.<ResponseField.Condition>emptyList()),
      ResponseField.forString("name", "name", null, false, Collections.<ResponseField.Condition>emptyList()),
      ResponseField.forString("start", "start", null, false, Collections.<ResponseField.Condition>emptyList()),
      ResponseField.forList("values", "values", null, false, Collections.<ResponseField.Condition>emptyList())
    };

    final @NotNull String __typename;

    final @NotNull String name;

    final @NotNull String start;

    final @NotNull List<Value> values;

    private transient volatile String $toString;

    private transient volatile int $hashCode;

    private transient volatile boolean $hashCodeMemoized;

    public Result(@NotNull String __typename, @NotNull String name, @NotNull String start,
        @NotNull List<Value> values) {
      this.__typename = Utils.checkNotNull(__typename, "__typename == null");
      this.name = Utils.checkNotNull(name, "name == null");
      this.start = Utils.checkNotNull(start, "start == null");
      this.values = Utils.checkNotNull(values, "values == null");
    }

    public @NotNull String __typename() {
      return this.__typename;
    }

    public @NotNull String name() {
      return this.name;
    }

    public @NotNull String start() {
      return this.start;
    }

    public @NotNull List<Value> values() {
      return this.values;
    }

    @SuppressWarnings({"rawtypes", "unchecked"})
    public ResponseFieldMarshaller marshaller() {
      return new ResponseFieldMarshaller() {
        @Override
        public void marshal(ResponseWriter writer) {
          writer.writeString($responseFields[0], __typename);
          writer.writeString($responseFields[1], name);
          writer.writeString($responseFields[2], start);
          writer.writeList($responseFields[3], values, new ResponseWriter.ListWriter() {
            @Override
            public void write(List items, ResponseWriter.ListItemWriter listItemWriter) {
              for (Object item : items) {
                listItemWriter.writeObject(((Value) item).marshaller());
              }
            }
          });
        }
      };
    }

    @Override
    public String toString() {
      if ($toString == null) {
        $toString = "Result{"
          + "__typename=" + __typename + ", "
          + "name=" + name + ", "
          + "start=" + start + ", "
          + "values=" + values
          + "}";
      }
      return $toString;
    }

    @Override
    public boolean equals(Object o) {
      if (o == this) {
        return true;
      }
      if (o instanceof Result) {
        Result that = (Result) o;
        return this.__typename.equals(that.__typename)
         && this.name.equals(that.name)
         && this.start.equals(that.start)
         && this.values.equals(that.values);
      }
      return false;
    }

    @Override
    public int hashCode() {
      if (!$hashCodeMemoized) {
        int h = 1;
        h *= 1000003;
        h ^= __typename.hashCode();
        h *= 1000003;
        h ^= name.hashCode();
        h *= 1000003;
        h ^= start.hashCode();
        h *= 1000003;
        h ^= values.hashCode();
        $hashCode = h;
        $hashCodeMemoized = true;
      }
      return $hashCode;
    }

    public static final class Mapper implements ResponseFieldMapper<Result> {
      final Value.Mapper valueFieldMapper = new Value.Mapper();

      @Override
      public Result map(ResponseReader reader) {
        final String __typename = reader.readString($responseFields[0]);
        final String name = reader.readString($responseFields[1]);
        final String start = reader.readString($responseFields[2]);
        final List<Value> values = reader.readList($responseFields[3], new ResponseReader.ListReader<Value>() {
          @Override
          public Value read(ResponseReader.ListItemReader listItemReader) {
            return listItemReader.readObject(new ResponseReader.ObjectReader<Value>() {
              @Override
              public Value read(ResponseReader reader) {
                return valueFieldMapper.map(reader);
              }
            });
          }
        });
        return new Result(__typename, name, start, values);
      }
    }
  }

  public static class Value {
    static final ResponseField[] $responseFields = {
      ResponseField.forString("__typename", "__typename", null, false, Collections.<ResponseField.Condition>emptyList()),
      ResponseField.forDouble("x", "x", null, false, Collections.<ResponseField.Condition>emptyList()),
      ResponseField.forCustomType("y", "y", null, false, CustomType.SIMULATIONRESULTVALUEY, Collections.<ResponseField.Condition>emptyList())
    };

    final @NotNull String __typename;

    final double x;

    final @NotNull Object y;

    private transient volatile String $toString;

    private transient volatile int $hashCode;

    private transient volatile boolean $hashCodeMemoized;

    public Value(@NotNull String __typename, double x, @NotNull Object y) {
      this.__typename = Utils.checkNotNull(__typename, "__typename == null");
      this.x = x;
      this.y = Utils.checkNotNull(y, "y == null");
    }

    public @NotNull String __typename() {
      return this.__typename;
    }

    public double x() {
      return this.x;
    }

    public @NotNull Object y() {
      return this.y;
    }

    @SuppressWarnings({"rawtypes", "unchecked"})
    public ResponseFieldMarshaller marshaller() {
      return new ResponseFieldMarshaller() {
        @Override
        public void marshal(ResponseWriter writer) {
          writer.writeString($responseFields[0], __typename);
          writer.writeDouble($responseFields[1], x);
          writer.writeCustom((ResponseField.CustomTypeField) $responseFields[2], y);
        }
      };
    }

    @Override
    public String toString() {
      if ($toString == null) {
        $toString = "Value{"
          + "__typename=" + __typename + ", "
          + "x=" + x + ", "
          + "y=" + y
          + "}";
      }
      return $toString;
    }

    @Override
    public boolean equals(Object o) {
      if (o == this) {
        return true;
      }
      if (o instanceof Value) {
        Value that = (Value) o;
        return this.__typename.equals(that.__typename)
         && Double.doubleToLongBits(this.x) == Double.doubleToLongBits(that.x)
         && this.y.equals(that.y);
      }
      return false;
    }

    @Override
    public int hashCode() {
      if (!$hashCodeMemoized) {
        int h = 1;
        h *= 1000003;
        h ^= __typename.hashCode();
        h *= 1000003;
        h ^= Double.valueOf(x).hashCode();
        h *= 1000003;
        h ^= y.hashCode();
        $hashCode = h;
        $hashCodeMemoized = true;
      }
      return $hashCode;
    }

    public static final class Mapper implements ResponseFieldMapper<Value> {
      @Override
      public Value map(ResponseReader reader) {
        final String __typename = reader.readString($responseFields[0]);
        final double x = reader.readDouble($responseFields[1]);
        final Object y = reader.readCustomType((ResponseField.CustomTypeField) $responseFields[2]);
        return new Value(__typename, x, y);
      }
    }
  }

  public static class Activity {
    static final ResponseField[] $responseFields = {
      ResponseField.forString("__typename", "__typename", null, false, Collections.<ResponseField.Condition>emptyList()),
      ResponseField.forDouble("duration", "duration", null, true, Collections.<ResponseField.Condition>emptyList()),
      ResponseField.forString("startTimestamp", "startTimestamp", null, false, Collections.<ResponseField.Condition>emptyList()),
      ResponseField.forString("type", "type", null, false, Collections.<ResponseField.Condition>emptyList()),
      ResponseField.forList("parameters", "parameters", null, false, Collections.<ResponseField.Condition>emptyList())
    };

    final @NotNull String __typename;

    final @Nullable Double duration;

    final @NotNull String startTimestamp;

    final @NotNull String type;

    final @NotNull List<Parameter> parameters;

    private transient volatile String $toString;

    private transient volatile int $hashCode;

    private transient volatile boolean $hashCodeMemoized;

    public Activity(@NotNull String __typename, @Nullable Double duration,
        @NotNull String startTimestamp, @NotNull String type, @NotNull List<Parameter> parameters) {
      this.__typename = Utils.checkNotNull(__typename, "__typename == null");
      this.duration = duration;
      this.startTimestamp = Utils.checkNotNull(startTimestamp, "startTimestamp == null");
      this.type = Utils.checkNotNull(type, "type == null");
      this.parameters = Utils.checkNotNull(parameters, "parameters == null");
    }

    public @NotNull String __typename() {
      return this.__typename;
    }

    public @Nullable Double duration() {
      return this.duration;
    }

    public @NotNull String startTimestamp() {
      return this.startTimestamp;
    }

    public @NotNull String type() {
      return this.type;
    }

    public @NotNull List<Parameter> parameters() {
      return this.parameters;
    }

    @SuppressWarnings({"rawtypes", "unchecked"})
    public ResponseFieldMarshaller marshaller() {
      return new ResponseFieldMarshaller() {
        @Override
        public void marshal(ResponseWriter writer) {
          writer.writeString($responseFields[0], __typename);
          writer.writeDouble($responseFields[1], duration);
          writer.writeString($responseFields[2], startTimestamp);
          writer.writeString($responseFields[3], type);
          writer.writeList($responseFields[4], parameters, new ResponseWriter.ListWriter() {
            @Override
            public void write(List items, ResponseWriter.ListItemWriter listItemWriter) {
              for (Object item : items) {
                listItemWriter.writeObject(((Parameter) item).marshaller());
              }
            }
          });
        }
      };
    }

    @Override
    public String toString() {
      if ($toString == null) {
        $toString = "Activity{"
          + "__typename=" + __typename + ", "
          + "duration=" + duration + ", "
          + "startTimestamp=" + startTimestamp + ", "
          + "type=" + type + ", "
          + "parameters=" + parameters
          + "}";
      }
      return $toString;
    }

    @Override
    public boolean equals(Object o) {
      if (o == this) {
        return true;
      }
      if (o instanceof Activity) {
        Activity that = (Activity) o;
        return this.__typename.equals(that.__typename)
         && ((this.duration == null) ? (that.duration == null) : this.duration.equals(that.duration))
         && this.startTimestamp.equals(that.startTimestamp)
         && this.type.equals(that.type)
         && this.parameters.equals(that.parameters);
      }
      return false;
    }

    @Override
    public int hashCode() {
      if (!$hashCodeMemoized) {
        int h = 1;
        h *= 1000003;
        h ^= __typename.hashCode();
        h *= 1000003;
        h ^= (duration == null) ? 0 : duration.hashCode();
        h *= 1000003;
        h ^= startTimestamp.hashCode();
        h *= 1000003;
        h ^= type.hashCode();
        h *= 1000003;
        h ^= parameters.hashCode();
        $hashCode = h;
        $hashCodeMemoized = true;
      }
      return $hashCode;
    }

    public static final class Mapper implements ResponseFieldMapper<Activity> {
      final Parameter.Mapper parameterFieldMapper = new Parameter.Mapper();

      @Override
      public Activity map(ResponseReader reader) {
        final String __typename = reader.readString($responseFields[0]);
        final Double duration = reader.readDouble($responseFields[1]);
        final String startTimestamp = reader.readString($responseFields[2]);
        final String type = reader.readString($responseFields[3]);
        final List<Parameter> parameters = reader.readList($responseFields[4], new ResponseReader.ListReader<Parameter>() {
          @Override
          public Parameter read(ResponseReader.ListItemReader listItemReader) {
            return listItemReader.readObject(new ResponseReader.ObjectReader<Parameter>() {
              @Override
              public Parameter read(ResponseReader reader) {
                return parameterFieldMapper.map(reader);
              }
            });
          }
        });
        return new Activity(__typename, duration, startTimestamp, type, parameters);
      }
    }
  }

  public static class Parameter {
    static final ResponseField[] $responseFields = {
      ResponseField.forString("__typename", "__typename", null, false, Collections.<ResponseField.Condition>emptyList()),
      ResponseField.forString("name", "name", null, false, Collections.<ResponseField.Condition>emptyList()),
      ResponseField.forCustomType("value", "value", null, true, CustomType.ACTIVITYINSTANCEPARAMETERVALUE, Collections.<ResponseField.Condition>emptyList())
    };

    final @NotNull String __typename;

    final @NotNull String name;

    final @Nullable Object value;

    private transient volatile String $toString;

    private transient volatile int $hashCode;

    private transient volatile boolean $hashCodeMemoized;

    public Parameter(@NotNull String __typename, @NotNull String name, @Nullable Object value) {
      this.__typename = Utils.checkNotNull(__typename, "__typename == null");
      this.name = Utils.checkNotNull(name, "name == null");
      this.value = value;
    }

    public @NotNull String __typename() {
      return this.__typename;
    }

    public @NotNull String name() {
      return this.name;
    }

    public @Nullable Object value() {
      return this.value;
    }

    @SuppressWarnings({"rawtypes", "unchecked"})
    public ResponseFieldMarshaller marshaller() {
      return new ResponseFieldMarshaller() {
        @Override
        public void marshal(ResponseWriter writer) {
          writer.writeString($responseFields[0], __typename);
          writer.writeString($responseFields[1], name);
          writer.writeCustom((ResponseField.CustomTypeField) $responseFields[2], value);
        }
      };
    }

    @Override
    public String toString() {
      if ($toString == null) {
        $toString = "Parameter{"
          + "__typename=" + __typename + ", "
          + "name=" + name + ", "
          + "value=" + value
          + "}";
      }
      return $toString;
    }

    @Override
    public boolean equals(Object o) {
      if (o == this) {
        return true;
      }
      if (o instanceof Parameter) {
        Parameter that = (Parameter) o;
        return this.__typename.equals(that.__typename)
         && this.name.equals(that.name)
         && ((this.value == null) ? (that.value == null) : this.value.equals(that.value));
      }
      return false;
    }

    @Override
    public int hashCode() {
      if (!$hashCodeMemoized) {
        int h = 1;
        h *= 1000003;
        h ^= __typename.hashCode();
        h *= 1000003;
        h ^= name.hashCode();
        h *= 1000003;
        h ^= (value == null) ? 0 : value.hashCode();
        $hashCode = h;
        $hashCodeMemoized = true;
      }
      return $hashCode;
    }

    public static final class Mapper implements ResponseFieldMapper<Parameter> {
      @Override
      public Parameter map(ResponseReader reader) {
        final String __typename = reader.readString($responseFields[0]);
        final String name = reader.readString($responseFields[1]);
        final Object value = reader.readCustomType((ResponseField.CustomTypeField) $responseFields[2]);
        return new Parameter(__typename, name, value);
      }
    }
  }
}
