{
  "name": "schemas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "quicktype -s schema -l ts --just-types --alphabetize-properties -o index.d.ts ./src/main/resources/*.json",
    "clean": "rimraf index.d.ts"
  },
  "dependencies": {
    "quicktype": "^15.0.174",
    "rimraf": "^2.6.3"
  }
}
