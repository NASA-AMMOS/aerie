{
  "name": "schemas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run clean && npm run buildjava && npm run buildts",
    "buildjava": "jsonschema2pojo -E -S -a NONE -c -p gov.nasa.jpl.aerie.schemas --source ./json --target src/main/java && mvn package -q",
    "buildts": "quicktype -s schema -l ts --just-types --alphabetize-properties -o index.d.ts ./json/*.json",
    "clean": "rimraf index.d.ts && rimraf src && rimraf target"
  },
  "dependencies": {
    "quicktype": "^15.0.174",
    "rimraf": "^2.6.3"
  }
}
