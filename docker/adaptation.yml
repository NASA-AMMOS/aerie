version: '3.3'
services:
  adaptation:
    build: ../adaptation/
    command: java -jar -Dspring.profiles.active=production adaptation.jar
    volumes:
      - adaptation_files:/usr/src/adaptation/adaptation_files
      - adaptation_logs:/var/log/adaptation
    ports:
      - "27182:27182"

volumes:
  adaptation_files: