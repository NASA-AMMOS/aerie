apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: plandev-dev
  name: plandev-merlin-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: plandev-merlin-worker
  template:
    metadata:
      labels:
        app: plandev-merlin-worker
    spec:
      containers:
        - name: plandev-merlin-worker
          image: ghcr.io/nasa-ammos/plandev-merlin-worker:develop
          ports:
            - containerPort: 8080
          resources: {}
          volumeMounts:
            - mountPath: /usr/src/app/merlin_file_store
              name: plandev-file-store
              readOnly: true
          env:
            - name: JAVA_OPTS
              value: |
                -Dorg.slf4j.simpleLogger.defaultLogLevel=INFO -Dorg.slf4j.simpleLogger.log.com.zaxxer.hikari=WARN -Dorg.slf4j.simpleLogger.logFile=System.err
            - name: PLANDEV_DB_PORT
              value: "5432"
            - name: PLANDEV_DB_HOST
              value: postgres
            - name: MERLIN_WORKER_LOCAL_STORE
              value: /usr/src/app/merlin_file_store
            - name: UNTRUE_PLAN_START
              value: "2000-01-01T11:58:55.816Z"
            - name: MERLIN_DB_USER
              valueFrom:
                secretKeyRef:
                  name: dev-env
                  key: MERLIN_USERNAME
            - name: MERLIN_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: dev-env
                  key: MERLIN_PASSWORD
      restartPolicy: Always
      volumes:
        - name: plandev-file-store
          persistentVolumeClaim:
            claimName: plandev-file-store
            readOnly: true
