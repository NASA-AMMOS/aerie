apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: plandev-ui-ingress
  namespace: plandev-dev
spec:
  rules:
    - http:
        paths:
          - backend:
              service:
                name: plandev-ui
                port:
                  number: 80
            path: /
            pathType: Prefix
