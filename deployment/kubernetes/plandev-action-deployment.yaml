apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: plandev-dev
  name: plandev-action
spec:
  replicas: 1
  selector:
    matchLabels:
      app: plandev-action
  template:
    metadata:
      labels:
        app: plandev-action
    spec:
      containers:
        - name: plandev-action
          image: ghcr.io/nasa-ammos/plandev-action:develop
          ports:
            - containerPort: 27186
          resources: {}
          volumeMounts:
            - mountPath: /usr/src/app/action_file_store
              name: aerie-file-store
          env:
            - name: HASURA_GRAPHQL_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: dev-env
                  key: HASURA_GRAPHQL_JWT_SECRET
            - name: LOG_FILE
              value: console
            - name: LOG_LEVEL
              value: warn
            - name: MERLIN_GRAPHQL_URL
              value: http://hasura:8080/v1/graphql
            - name: AERIE_DB_PORT
              value: "5432"
            - name: AERIE_DB_HOST
              value: postgres
            - name: ACTION_COOKIE_NAMES
              valueFrom:
                secretKeyRef:
                  name: dev-env
                  key: ACTION_COOKIE_NAMES
                  optional: true
            - name: ACTION_CORS_ALLOWED_ORIGIN
              valueFrom:
                secretKeyRef:
                  name: dev-env
                  key: ACTION_CORS_ALLOWED_ORIGIN
                  optional: true
            - name: ACTION_LOCAL_STORE
              value: /usr/src/app/action_file_store
            - name: ACTION_DB_USER
              valueFrom:
                secretKeyRef:
                  name: dev-env
                  key: SEQUENCING_USERNAME
            - name: ACTION_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: dev-env
                  key: SEQUENCING_PASSWORD
      restartPolicy: Always
      volumes:
        - name: aerie-file-store
          persistentVolumeClaim:
            claimName: aerie-file-store
