table:
  name: scheduling_goal
  schema: public
array_relationships:
- name: analyses
  using:
    foreign_key_constraint_on:
      column: goal_id
      table:
        name: scheduling_goal_analysis
        schema: public
