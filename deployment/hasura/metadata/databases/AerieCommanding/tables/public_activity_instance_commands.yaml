table:
  name: activity_instance_commands
  schema: public
object_relationships:
- name: expansion_run
  using:
    foreign_key_constraint_on: expansion_run_id
# TODO: Add relationship to activities through the expansion run -> simulation_dataset_id -> simulated activity instance
