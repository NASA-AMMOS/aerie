table:
  name: users
  schema: metadata
configuration:
  custom_name: "users"
select_permissions:
  - role: user
    permission:
      columns: [id, username]
      filter: {}
      allow_aggregations: true
  - role: viewer
    permission:
      columns: [id, username]
      filter: {}
      allow_aggregations: true
