table:
  name: constraint_tags
  schema: metadata
configuration:
  custom_name: "constraint_tags"
object_relationships:
  - name: constraint
    using:
      foreign_key_constraint_on: constraint_id
  - name: tag
    using:
      foreign_key_constraint_on: tag_id
select_permissions:
  - role: aerie_admin
    permission:
      columns: [constraint_id, tag_id]
      filter: {}
      allow_aggregations: true
  - role: user
    permission:
      columns: [constraint_id, tag_id]
      filter: {}
      allow_aggregations: true
  - role: viewer
    permission:
      columns: [constraint_id, tag_id]
      filter: {}
      allow_aggregations: true
insert_permissions:
  - role: aerie_admin
    permission:
      columns: [constraint_id, tag_id]
      check: {}
  - role: user
    permission:
      columns: [constraint_id, tag_id]
      check: {"constraint":{"owner":{"_eq":"X-Hasura-User-Id"}}}
delete_permissions:
  - role: aerie_admin
    permission:
      filter: {}
  - role: user
    permission:
      filter: {"constraint":{"owner":{"_eq":"X-Hasura-User-Id"}}}
