table:
  name: resource_type
  definition: public
select_permissions:
  - role: aerie_admin
    permission:
      columns: [model_id, name, definition]
      filter: {}
      allow_aggregations: true
  - role: user
    permission:
      columns: [model_id, name, definition]
      filter: {}
      allow_aggregations: true
  - role: viewer
    permission:
      columns: [model_id, name, definition]
      filter: {}
      allow_aggregations: true
delete_permissions:
  - role: aerie_admin
    permission:
      filter: {}
