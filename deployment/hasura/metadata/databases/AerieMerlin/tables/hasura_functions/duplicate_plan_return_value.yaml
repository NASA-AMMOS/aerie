table:
  name: duplicate_plan_return_value
  schema: hasura_functions
select_permissions:
  - role: aerie_admin
    permission:
      columns: [new_plan_id]
      filter: {}
  - role: user
    permission:
      columns: [new_plan_id]
      filter: {}
