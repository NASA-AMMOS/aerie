table:
  name: get_plan_history_return_value
  schema: hasura_functions
select_permissions:
  - role: aerie_admin
    permission:
      columns: [plan_id]
      filter: {}
  - role: user
    permission:
      columns: [plan_id]
      filter: {}
