table:
  name: derivation_group_comp
  schema: merlin
configuration:
  custom_name: "derivation_group_comp"
object_relationships: # nothing for the arrays, can't grab string values and associate as ids in hasura. but shouldn't need to...
  - name: derivation_group
    using:
      manual_configuration:
        remote_table:
          name: derivation_group
          schema: merlin
        column_mapping:
          name: name
select_permissions:
  - role: aerie_admin
    permission:
      columns: '*'
      filter: {}
      allow_aggregations: true
  - role: user
    permission:
      columns: '*'
      filter: {}
      allow_aggregations: true
  - role: viewer
    permission:
      columns: '*'
      filter: {}
      allow_aggregations: true
